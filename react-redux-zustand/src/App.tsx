import { Player } from "./pages/Player";

import './styles/global.css';

export const App = () => {
  return (
    // <ReduxProvider store={store}>
    //   <Player />
    // </ReduxProvider>
    <Player />
  );
}